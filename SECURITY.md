# 安全说明

validx 将以下情况视为安全缺陷:

- 规则解析或校验过程 panic(拒绝服务);
- 正则表达式拒绝服务(ReDoS):内置 regexp 规则由调用方提供,
  高危正则可拖慢校验;
- 错误消息泄露敏感字段值(如密码确认比较失败时输出明文)。

## 报告漏洞

- 请勿在公开 issue 中披露漏洞细节;
- 通过邮件 [lcylpzls@qq.com](mailto:lcylpzls@qq.com) 联系维护者,
  并在标题注明 `[Security]`;
- 修复发布前我们会与报告者保持沟通;发布后可应要求公开致谢。

## 安全使用建议

- 业务侧对用户提供的 regexp 规则(动态配置)谨慎使用,
  优先使用内置格式规则(email / uuid / url / ip);
- 错误信息默认不含字段值;自定义规则请勿将敏感值拼入错误消息;
- 校验失败路径构造 errx 错误(含调用栈),敏感场景可
  `errx.SetStackCapture(false)` 全局关闭。
